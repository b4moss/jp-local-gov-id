# 全国地方公共団体コードヘルパ 開発ロードマップ

仕様書（[main.md](./main.md)）に基づく開発ステップ。

## ~v0.5.0

リリース済み

## v0.6.0

完了

### app

#### 政令指定都市オプションの追加

- [x] 市のみ出力
- [x] 市は出力せず、区のみ出力
- [x] 両方とも出力（現行、デフォルト）

### doc site

- [x] 少しだけコンテンツを見せつつ、展開ボタンを押すと、コンテンツを最後まで見せるボックス型UI
  - サンプルページの、ソースコードは、これで包む
- [x] サンプルページ
  - 住所入力
    - 一般的な住所入力風に見せる
    - 都道府県選択（現状OK）
    - 市区町村選択（現状OK）
    - 町名・番地（テキストフィールドだけ追加。挙動は何もなし）
    - 建物名（テキストフィールドだけ追加。挙動は何もなし）
  - 市区町村バリデーション
    - 一般的な住所入力風に見せる
    - 都道府県選択（現状OK）
    - 市区町村文字列入力（現状OK）
    - 町名・番地（テキストフィールドだけ追加。挙動は何もなし）
    - 建物名（テキストフィールドだけ追加。挙動は何もなし）

## v1.0.0

完了（GA / Issue #121）。data / app `1.0.0`。

## v1.1.0

完了

- エラー／警告メッセージの JSONC 分離（#85）
- キャッシュを `@b4moss/cachian` に外出し、`purgeCache` 公開（#94）

## v1.2.0

完了

- `createLocalGovClient` 初期ロード ≤25KB（#93 / follow-up）
  - 検索実装・cachian の動的 import
  - メッセージカタログ分割、binary decode 直参照など
- 計測: 初期 minify ≈ 24339（`docs/client-bundle-93.md`）

----

以上
