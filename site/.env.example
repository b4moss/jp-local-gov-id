# Google Tag Manager container ID (optional)
# Leave empty to disable tagging. For GitHub Pages SSG, set this as a repo Actions
# variable so it is available at build time.
# Local `nuxt dev` never loads GTM even if this is set (see plugins/google-tag-manager.client.ts).
NUXT_PUBLIC_SCRIPTS_GOOGLE_TAG_MANAGER_ID=
