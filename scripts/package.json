{
  "name": "jp-local-gov-id-scripts",
  "private": true,
  "version": "0.0.0",
  "description": "Excel → CSV / 独自バイナリ生成スクリプト（Node.js 専用・配布対象外）",
  "type": "module",
  "scripts": {
    "generate": "tsx generate.ts"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "exceljs": "^4.4.0",
    "tsx": "^4.20.3"
  }
}
